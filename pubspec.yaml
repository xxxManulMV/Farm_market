name: farm_market
description: Chechen farm marketplace
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0-283.0.dev <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  animated_item_picker: ^2.0.0
  cupertino_listview: ^2.0.0
  flashy_tab_bar2: ^0.0.5
  awesome_card: ^1.1.7
  shaky_animated_listview: ^0.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true